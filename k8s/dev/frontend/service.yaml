apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: dev

spec:
  type: ClusterIP

  selector:
    app: frontend

  ports:
    - port: 80          # Service port
      targetPort: 80    # nginx / frontend port